{
  "build": {
    "commands": [
      "composer install",
      "php artisan key:generate",
      "php artisan migrate --force"
    ]
  },
  "start": "php artisan serve --host=0.0.0.0 --port=8080"
}
